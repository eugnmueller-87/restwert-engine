# runtime data (regenerate with `python -m restwert all`)
data/*.duckdb
data/*.duckdb.wal
data/raw_csv/
data/SYNTHETIC.md
# data lake: the landing files under data/lake/raw are the mock data (regenerated by `all`,
# whether to commit them is the owner's call); the parquet mirrors are derived by `export --lake`
data/lake/bronze/
data/lake/silver/
data/lake/gold/
outputs/*
!outputs/.gitkeep

# python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
web/dist-cockpit/
.pytest_cache/
.venv/
venv/
.mypy_cache/
.ruff_cache/

# editors and OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# landing files are regenerated by `python -m restwert all` (14 MB, 260 files); real exports never belong in git
data/lake/raw/

# page build: node modules for the jsdom tests and the rendered text of the last test run
web/node_modules/
web/out/
