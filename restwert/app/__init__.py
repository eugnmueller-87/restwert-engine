"""Compatibility entry point: ``streamlit run restwert/app/streamlit_app.py``.

The dashboard itself lives in ``restwert/dashboard/`` (SPEC 8.3). This package only
forwards to it so both paths open the same app.

THE MODEL ADVISES, DETERMINISTIC CODE DECIDES, A NAMED HUMAN OWNS EVERY THRESHOLD.
"""
